import React from 'react';
import styled from 'styled-components';

const Introduce = (): JSX.Element => {
  return <IntroduceSection></IntroduceSection>;
};

const IntroduceSection = styled.section`
  height: 500vh;
`;

export default Introduce;
